<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/sub_nav_graph_2"
    app:startDestination="@id/subFragment2">
    <!-- GLOBAL ACTION -->
    <action
        android:id="@+id/action_to_nav_graph_2"
        app:destination="@id/sub_nav_graph_2">
        <argument
            android:name="label"
            app:argType="string" />
    </action>
    <fragment
        android:id="@+id/subFragment2"
        android:name="com.example.myapplication.SubFragment2"
        android:label="fragment_sub2"
        tools:layout="@layout/fragment_sub2">
        <argument
            android:name="label"
            app:argType="string" />
    </fragment>
</navigation>
